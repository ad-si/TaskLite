-- Do not manually edit this file, it was auto-generated by dillonkearns/elm-graphql
-- https://github.com/dillonkearns/elm-graphql
module Api.Enum.Tasks_column exposing (..)

import Json.Decode as Decode exposing (Decoder)


{-| This enum contains a variant for each column in the table
-}
type Tasks_column
    = Rowid
    | Ulid
    | Body
    | Modified_utc
    | Awake_utc
    | Ready_utc
    | Waiting_utc
    | Review_utc
    | Due_utc
    | Closed_utc
    | State
    | Group_ulid
    | Repetition_duration
    | Recurrence_duration
    | Priority_adjustment
    | User
    | Metadata
list : List Tasks_column
list =
    [Rowid, Ulid, Body, Modified_utc, Awake_utc, Ready_utc, Waiting_utc, Review_utc, Due_utc, Closed_utc, State, Group_ulid, Repetition_duration, Recurrence_duration, Priority_adjustment, User, Metadata]
decoder : Decoder Tasks_column
decoder =
    Decode.string
        |> Decode.andThen
            (\string ->
                case string of
                    "rowid" ->
                        Decode.succeed Rowid

                    "ulid" ->
                        Decode.succeed Ulid

                    "body" ->
                        Decode.succeed Body

                    "modified_utc" ->
                        Decode.succeed Modified_utc

                    "awake_utc" ->
                        Decode.succeed Awake_utc

                    "ready_utc" ->
                        Decode.succeed Ready_utc

                    "waiting_utc" ->
                        Decode.succeed Waiting_utc

                    "review_utc" ->
                        Decode.succeed Review_utc

                    "due_utc" ->
                        Decode.succeed Due_utc

                    "closed_utc" ->
                        Decode.succeed Closed_utc

                    "state" ->
                        Decode.succeed State

                    "group_ulid" ->
                        Decode.succeed Group_ulid

                    "repetition_duration" ->
                        Decode.succeed Repetition_duration

                    "recurrence_duration" ->
                        Decode.succeed Recurrence_duration

                    "priority_adjustment" ->
                        Decode.succeed Priority_adjustment

                    "user" ->
                        Decode.succeed User

                    "metadata" ->
                        Decode.succeed Metadata

                    _ ->
                        Decode.fail ("Invalid Tasks_column type, " ++ string ++ " try re-running the @dillonkearns/elm-graphql CLI ")
        )
        

{-| Convert from the union type representing the Enum to a string that the GraphQL server will recognize.
-}
toString : Tasks_column -> String
toString enum____ =
    case enum____ of
        Rowid ->
                "rowid"


        Ulid ->
                "ulid"


        Body ->
                "body"


        Modified_utc ->
                "modified_utc"


        Awake_utc ->
                "awake_utc"


        Ready_utc ->
                "ready_utc"


        Waiting_utc ->
                "waiting_utc"


        Review_utc ->
                "review_utc"


        Due_utc ->
                "due_utc"


        Closed_utc ->
                "closed_utc"


        State ->
                "state"


        Group_ulid ->
                "group_ulid"


        Repetition_duration ->
                "repetition_duration"


        Recurrence_duration ->
                "recurrence_duration"


        Priority_adjustment ->
                "priority_adjustment"


        User ->
                "user"


        Metadata ->
                "metadata"


{-| Convert from a String representation to an elm representation enum.
This is the inverse of the Enum `toString` function. So you can call `toString` and then convert back `fromString` safely.

    Swapi.Enum.Episode.NewHope
        |> Swapi.Enum.Episode.toString
        |> Swapi.Enum.Episode.fromString
        == Just NewHope

This can be useful for generating Strings to use for <select> menus to check which item was selected.

-}
fromString : String -> Maybe Tasks_column
fromString enumString____ =
    case enumString____ of
        "rowid" ->
                Just Rowid


        "ulid" ->
                Just Ulid


        "body" ->
                Just Body


        "modified_utc" ->
                Just Modified_utc


        "awake_utc" ->
                Just Awake_utc


        "ready_utc" ->
                Just Ready_utc


        "waiting_utc" ->
                Just Waiting_utc


        "review_utc" ->
                Just Review_utc


        "due_utc" ->
                Just Due_utc


        "closed_utc" ->
                Just Closed_utc


        "state" ->
                Just State


        "group_ulid" ->
                Just Group_ulid


        "repetition_duration" ->
                Just Repetition_duration


        "recurrence_duration" ->
                Just Recurrence_duration


        "priority_adjustment" ->
                Just Priority_adjustment


        "user" ->
                Just User


        "metadata" ->
                Just Metadata

        _ ->
                Nothing
