name: Haskell Stack CI

on: [push]

jobs:
  build:
    name: ${{ matrix.os }} (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: x86_64
            runner: ubuntu-24.04
          - os: linux
            arch: arm64
            runner: ubuntu-24.04-arm
          - os: macos
            arch: x86_64
            runner: macos-15-intel
          - os: macos
            arch: arm64
            runner: macos-26

    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Lua
        uses: leafo/gh-actions-lua@v13

      - name: Setup Stack
        uses: haskell-actions/setup@v2
        with:
          enable-stack: true
          stack-no-global: true

      - name: Cache Stack build files
        uses: actions/cache@v6
        with:
          path: |
            ~/.stack
            .stack-work
          key: >-
            ${{ matrix.os }}-${{ matrix.arch }}-stack-${{
            hashFiles('stack.yaml.lock', 'tasklite/package.yaml',
            'tasklite-core/package.yaml') }}-${{ github.sha }}
          restore-keys: |
            ${{ matrix.os }}-${{ matrix.arch }}-stack-${{ hashFiles('stack.yaml.lock', 'tasklite/package.yaml', 'tasklite-core/package.yaml') }}-
            ${{ matrix.os }}-${{ matrix.arch }}-stack-

      - name: Install TaskLite CLI tool
        # Retried, because fetching from hackage.haskell.org
        # intermittently fails on GitHub's runners
        run: |
          for attempt in 1 2 3; do
            stack --local-bin-path ./dist install tasklite && exit 0
            echo "::warning::stack install failed (attempt $attempt of 3)"
            sleep 30
          done
          exit 1

      - name: Run test suite
        run: stack test

      - name: Test adding of tasks
        run: ./dist/tasklite add Buy milk

      - name: Test listing of tasks
        run: ./dist/tasklite

      - name: Upload Release
        uses: actions/upload-artifact@v7
        with:
          path: ./dist/tasklite
          name: tasklite_${{ matrix.os }}_${{ matrix.arch }}

  docker:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Git Repository
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # Only `main` and `v*` tags are published to Docker Hub.
      # Every other branch merely builds the image to verify the Dockerfile.
      - name: Determine image tags
        id: meta
        env:
          REF: ${{ github.ref }}
          REF_NAME: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
        run: |
          if [ "$REPO" != "ad-si/TaskLite" ]; then
            # Forks don't have the Docker Hub secrets
            echo 'push=false' >> "$GITHUB_OUTPUT"
            echo 'tags=adius/tasklite:ci' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          case "$REF" in
            refs/heads/main)
              echo 'push=true' >> "$GITHUB_OUTPUT"
              echo 'tags=adius/tasklite:latest' >> "$GITHUB_OUTPUT"
              ;;
            refs/tags/v*)
              echo 'push=true' >> "$GITHUB_OUTPUT"
              echo "tags=adius/tasklite:$REF_NAME,adius/tasklite:latest" \
                >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo 'push=false' >> "$GITHUB_OUTPUT"
              echo 'tags=adius/tasklite:ci' >> "$GITHUB_OUTPUT"
              ;;
          esac

      - name: Login to DockerHub
        if: steps.meta.outputs.push == 'true'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: ${{ steps.meta.outputs.push == 'true' }}
          tags: ${{ steps.meta.outputs.tags }}

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  # windows:
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@v7

  #     - name: Install stack
  #       shell: cmd
  #       run: choco install haskell-stack

  #     - name: Install dependencies
  #       run: stack install tasklite

  #     - name: Upload Windows Release
  #       uses: actions/upload-artifact@v7
  #       with:
  #         path: '%APPDATA%\local\bin\tasklite.exe'
  #         name: tasklite_windows_x86_64.exe

  # publish:
  #   needs: build
  #   runs-on: ubuntu-18.04
  #   steps:
  #     - name: Set env with output of `git describe`
  #       run: echo ::set-env name=GIT_DESCRIBE::$(git describe)
  #
  #     - name: Create Release
  #       id: create_release
  #       uses: actions/create-release@v1
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         tag_name: ${{ env.GIT_DESCRIBE }}
  #         release_name: Release ${{ env.GIT_DESCRIBE }}
  #         body: ${{ github.event.commits[0].message }}
  #         draft: false
  #         prerelease: false
